import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  ring: {
    borderColor: 'rgba(70, 100, 166, 0.2)',
    borderWidth: 1, 
    padding: 30, 
    borderRadius: 200,
  },
});

export default styles;